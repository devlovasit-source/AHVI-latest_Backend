fastapi==0.115.6
uvicorn[standard]==0.32.1
celery==5.4.0
redis==5.2.0
requests==2.32.3
httpx==0.28.1
sentry-sdk[fastapi]==2.18.0
deep-translator==1.11.4
appwrite==17.0.0
minio==7.2.10
qdrant-client==1.12.1
python-dotenv==1.0.1
numpy==2.1.3
opencv-python-headless==4.10.0.84
scikit-learn==1.5.2
Pillow==11.0.0
firebase-admin==6.6.0
mediapipe==0.10.14
python-multipart>=0.0.18
google-genai==1.16.1

# ──────────────────────────────────────────────────────────────────────────
# OPTIONAL: semantic retrieval (sentence-transformers + torch ≈ 700MB image).
# Disabled by default to keep cold-start fast and Cloud Run image lean.
# services.embedding_service.embeddings_enabled() returns False when this is
# missing, and brain.outfit_pipeline._semantic_retrieval() short-circuits to
# an empty result. The 62s production style-flow latency was traced to this
# path being loaded but uncached — leave commented unless you also size up
# Cloud Run memory to ≥ 4Gi.
#
# sentence-transformers==3.0.1
# ──────────────────────────────────────────────────────────────────────────
